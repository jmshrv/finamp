import 'package:flutter/material.dart';

class AddDownloadLocationDialog extends StatelessWidget {
  const AddDownloadLocationDialog({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AlertDialog();
  }
}
